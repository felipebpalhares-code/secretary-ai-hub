# Caddy · reverse proxy + HTTPS automático Let's Encrypt
# Variáveis vêm do docker-compose.prod.yml (DOMAIN_HUB, DOMAIN_API, ACME_EMAIL)

{
	email {$ACME_EMAIL}
}

# Frontend (Next.js)
{$DOMAIN_HUB} {
	encode gzip zstd
	reverse_proxy frontend:3000

	# Cache de assets estáticos
	@static {
		path /_next/static/* /favicon.ico /robots.txt
	}
	header @static Cache-Control "public, max-age=31536000, immutable"

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
		-Server
	}
}

# Backend API (FastAPI + WebSocket)
{$DOMAIN_API} {
	encode gzip
	reverse_proxy backend:8000

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "no-referrer"
		-Server
	}
}

# Evolution API (WhatsApp) — adicionar quando ativar perfil "wa"
# {$DOMAIN_WA} {
# 	encode gzip
# 	reverse_proxy evolution:8080
# 	header { Strict-Transport-Security "max-age=31536000; includeSubDomains"; -Server }
# }
