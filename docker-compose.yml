name: felipe-hub

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: felipe-backend
    ports:
      - "8000:8000"
    environment:
      # LLMs (preencha pelo menos um)
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}

      # Cofre criptografado (gere uma vez com:
      # docker run --rm python:3.12-slim sh -c "pip install -q cryptography && python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'")
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-}

      # WhatsApp via Evolution API (opcional — perfil "wa")
      EVOLUTION_API_URL: ${EVOLUTION_API_URL:-http://evolution:8080}
      EVOLUTION_API_KEY: ${EVOLUTION_API_KEY:-}
      EVOLUTION_INSTANCE: ${EVOLUTION_INSTANCE:-felipe-hub}
      FELIPE_WHATSAPP_NUMBER: ${FELIPE_WHATSAPP_NUMBER:-}

      # Pluggy Open Finance (opcional)
      PLUGGY_CLIENT_ID: ${PLUGGY_CLIENT_ID:-}
      PLUGGY_CLIENT_SECRET: ${PLUGGY_CLIENT_SECRET:-}
      PLUGGY_BASE_URL: ${PLUGGY_BASE_URL:-https://api.pluggy.ai}

      # Telegram / Discord (opcionais)
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      DISCORD_BOT_TOKEN: ${DISCORD_BOT_TOKEN:-}
      DISCORD_GUILD_ID: ${DISCORD_GUILD_ID:-}

      # CPF.CNPJ — busca empresas vinculadas a um CPF (pacote 15)
      # default = token de teste público (retorna dados fictícios).
      # Em produção: cadastrar em https://www.cpfcnpj.com.br/register/ e setar a chave aqui.
      CPF_LOOKUP_API_KEY: ${CPF_LOOKUP_API_KEY:-5ae973d7a997af13f0aaf2bf60e65803}

      # Google OAuth (Contatos / Gmail / Calendar)
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-http://localhost:8000/api/auth/google/callback}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}

      # CORS — frontend Next.js
      CORS_ORIGINS: "http://localhost:3000,http://127.0.0.1:3000"

      # Banco SQLite — persiste em volume
      DATABASE_PATH: /data/felipe_hub.db

      # ChromaDB (RAG dos agentes) — rede interna Docker
      CHROMA_HOST: ${CHROMA_HOST:-chroma}
      CHROMA_PORT: ${CHROMA_PORT:-8000}

      # Cache do modelo de embeddings (sentence-transformers/all-MiniLM-L6-v2)
      # — persistido em volume pra não rebaixar a cada container start
      SENTENCE_TRANSFORMERS_HOME: /data/st-cache
      HF_HOME: /data/hf-cache
    volumes:
      # Hot reload durante desenvolvimento (opcional — comente em produção)
      - ./backend:/app
      # Banco persistente + cache de modelos + uploads
      - hub-data:/data
    depends_on:
      chroma:
        condition: service_started
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ChromaDB — vector store usado pelo RAG dos agentes IA.
  # Backend conecta via http://chroma:8000 (rede Docker interna).
  # Host expõe na 8001 só para inspeção manual.
  chroma:
    # Pin igual à versão do client Python em backend/requirements.txt
    # (versões diferentes quebram em "KeyError: '_type'" na criação de collection).
    image: chromadb/chroma:0.5.23
    container_name: felipe-chroma
    ports:
      - "8001:8000"
    volumes:
      - ./backend/chroma_data:/chroma/chroma
    restart: unless-stopped

  # Evolution API (WhatsApp) — perfil opcional
  # Subir com: docker compose --profile wa up
  evolution:
    image: atendai/evolution-api:homolog
    container_name: felipe-evolution
    ports:
      - "8080:8080"
    profiles: ["wa"]
    depends_on:
      evolution-redis:
        condition: service_started
      evolution-postgres:
        condition: service_started
    environment:
      AUTHENTICATION_API_KEY: ${EVOLUTION_API_KEY:-}
      AUTHENTICATION_EXPOSE_IN_FETCH_INSTANCES: "true"
      LANGUAGE: "pt-BR"
      CONFIG_SESSION_PHONE_CLIENT: "Felipe Hub"
      CONFIG_SESSION_PHONE_NAME: "Chrome"
      LOG_LEVEL: "ERROR,WARN,DEBUG,INFO,LOG,VERBOSE,DARK,WEBHOOKS,WEBSOCKET"
      LOG_BAILEYS: "debug"
      LOG_COLOR: "false"

      # Postgres (provider via Prisma)
      DATABASE_PROVIDER: postgresql
      DATABASE_CONNECTION_URI: "postgresql://evolution:evolution@evolution-postgres:5432/evolution?schema=public"
      DATABASE_CONNECTION_CLIENT_NAME: evolution_exchange
      DATABASE_SAVE_DATA_INSTANCE: "true"
      DATABASE_SAVE_DATA_NEW_MESSAGE: "true"
      DATABASE_SAVE_MESSAGE_UPDATE: "true"
      DATABASE_SAVE_DATA_CONTACTS: "true"
      DATABASE_SAVE_DATA_CHATS: "true"
      DATABASE_SAVE_DATA_LABELS: "true"
      DATABASE_SAVE_DATA_HISTORIC: "true"

      # Cache Redis
      CACHE_REDIS_ENABLED: "true"
      CACHE_REDIS_URI: "redis://evolution-redis:6379/6"
      CACHE_REDIS_PREFIX_KEY: evolution_v2
      CACHE_LOCAL_ENABLED: "false"

      # Webhook (Evolution chama nosso backend quando chega mensagem)
      WEBHOOK_GLOBAL_URL: "http://backend:8000/api/connections/webhooks/whatsapp"
      WEBHOOK_GLOBAL_ENABLED: "true"
      WEBHOOK_GLOBAL_WEBHOOK_BY_EVENTS: "false"
      WEBHOOK_EVENTS_MESSAGES_UPSERT: "true"
      WEBHOOK_EVENTS_CONNECTION_UPDATE: "true"
      WEBHOOK_EVENTS_QRCODE_UPDATED: "true"
    volumes:
      - evolution-store:/evolution/store
      - evolution-instances:/evolution/instances
    restart: unless-stopped

  evolution-postgres:
    image: postgres:15-alpine
    container_name: felipe-evolution-postgres
    profiles: ["wa"]
    environment:
      POSTGRES_USER: evolution
      POSTGRES_PASSWORD: evolution
      POSTGRES_DB: evolution
    volumes:
      - evolution-postgres-data:/var/lib/postgresql/data
    restart: unless-stopped

  evolution-redis:
    image: redis:7-alpine
    container_name: felipe-evolution-redis
    profiles: ["wa"]
    volumes:
      - evolution-redis-data:/data
    restart: unless-stopped

volumes:
  hub-data:
  evolution-store:
  evolution-instances:
  evolution-postgres-data:
  evolution-redis-data:
